package it.polimi.ingsw.view.GUI;

public class GuiClient {
    //when one or more connection gets established
    //gui client should do the transition to the lobby window
    //when a lobby is full should do the transition to Santorini Board window
    //deal with reconnection/disconection issues (?)
}
